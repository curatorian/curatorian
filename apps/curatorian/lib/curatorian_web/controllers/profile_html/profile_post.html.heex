<Layouts.app
  flash={@flash}
  current_scope={@current_scope}
>
  <ProfileLayouts.profile_layout
    user={@user}
    current_scope={@current_scope}
    active_tab={@active_tab}
  >
    <h5 class="text-right dark:text-white">
      Semua Post dari {if @user == nil, do: @user.username, else: @user.fullname || @user.username}
    </h5>

    <section class="mt-7">
      <%= if length(@posts) > 0 do %>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <%= for post <- @posts do %>
            <div class="bg-white dark:bg-gray-700 rounded-2xl p-5 h-full">
              <img
                src={if post.image_url, do: post.image_url, else: "/images/lib.webp"}
                class="w-full max-h-56 object-cover shadow-lg rounded-xl"
              />
              <div class="flex justify-between items-center my-8 px-7 text-gray-500 dark:text-gray-400 text-xs">
                <p>by {post.user.fullname || post.user.username}</p>

                <p>{convert_time_zone_to_indonesia(post.inserted_at)}</p>
              </div>

              <div class="px-7 h-full">
                <.link
                  navigate={~p"/#{post.user.username}/posts/#{post.slug}"}
                  class="no-underline hover:underline"
                >
                  <h4 class="text-violet-500 dark:text-violet-300 -mb-2">{post.title}</h4>
                </.link>
                <p class="mt-5 text-gray-500 dark:text-gray-400 text-justify">
                  {trim_description(post.content, 120)}
                </p>

                <div class="my-10">
                  <.link
                    navigate={~p"/#{post.user.username}/posts/#{post.slug}"}
                    class="btn-primary no-underline"
                  >
                    Baca &rightarrow;
                  </.link>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="text-right text-gray-500 dark:text-gray-400 italic">
          Curatorian ini belum menulis post.
        </p>
      <% end %>
    </section>
  </ProfileLayouts.profile_layout>
</Layouts.app>
