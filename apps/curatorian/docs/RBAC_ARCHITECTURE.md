# RBAC System Architecture

## Database Schema

```
┌─────────────────┐
│     USERS       │
├─────────────────┤
│ id              │◄─────┐
│ email           │      │
│ username        │      │
│ role_id         │──────┼──────┐
│ ...             │      │      │
└─────────────────┘      │      │
                         │      │
┌─────────────────┐      │      │
│  USER_PROFILES  │      │      │
├─────────────────┤      │      │
│ id              │      │      │
│ user_id         │──────┘      │
│ fullname        │             │
│ bio             │             │
│ ...             │             │
└─────────────────┘             │
                                │
┌─────────────────┐             │
│     ROLES       │◄────────────┘
├─────────────────┤
│ id              │◄────────────┐
│ name            │             │
│ slug            │             │
│ description     │             │
│ is_system_role  │             │
│ priority        │             │
└─────────────────┘             │
        ▲                        │
        │                        │
        │                        │
┌───────┴─────────────┐          │
│  ROLE_PERMISSIONS   │          │
├─────────────────────┤          │
│ id                  │          │
│ role_id             │──────────┘
│ permission_id       │──────────┐
└─────────────────────┘          │
                                 │
┌─────────────────┐              │
│  PERMISSIONS    │◄─────────────┘
├─────────────────┤
│ id              │
│ name            │
│ slug            │
│ resource        │
│ action          │
│ description     │
└─────────────────┘
```

## System Flow

```
┌────────────────────────────────────────────────────────────┐
│                     USER MAKES REQUEST                      │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│              AUTHENTICATION (UserAuth)                      │
│  - Checks if user is logged in                             │
│  - Loads current_scope with user data                      │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│            AUTHORIZATION (CuratorianWeb.Authorization)     │
│  - Checks if user has required permission/role             │
│  - Uses: on_mount, plugs, or template helpers              │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
         ┌───────┴────────┐
         │                │
    ✅ ALLOWED      ❌ DENIED
         │                │
         ▼                ▼
    CONTINUE        REDIRECT TO
    REQUEST         DASHBOARD
```

## Authorization Check Flow

```
User requests protected resource
         │
         ▼
┌─────────────────────┐
│ Load User with Role │
│   (preload :role)   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  Check Permission   │
│   via role.slug     │
└──────────┬──────────┘
           │
           ▼
    ┌──────┴──────┐
    │             │
   YES           NO
    │             │
    ▼             ▼
 GRANT         DENY
 ACCESS      ACCESS
```

## Role Hierarchy

```
┌─────────────────────────────────────────┐
│         SUPER ADMIN (Priority: 100)     │
│  ✓ ALL Permissions                      │
│  ✓ Can manage roles & permissions       │
│  ✓ System role (cannot delete)          │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│          MANAGER (Priority: 80)         │
│  ✓ User management                      │
│  ✓ Content management                   │
│  ✓ Moderation                           │
│  ✗ Cannot manage roles                  │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│          CURATOR (Priority: 50)         │
│  ✓ Create blogs & organizations         │
│  ✓ Manage own content                   │
│  ✓ Publish content                      │
│  ✗ Cannot manage others' content        │
└─────────────────┬───────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────┐
│            USER (Priority: 10)          │
│  ✓ Read access                          │
│  ✓ Comment                              │
│  ✗ Cannot create content                │
└─────────────────────────────────────────┘
```

## Permission Structure by Resource

```
USERS
├── users:read         (View user profiles)
├── users:create       (Create new users)
├── users:update       (Update user info)
├── users:delete       (Delete users)
└── users:manage       (Full user management)

BLOGS
├── blogs:read         (View blogs)
├── blogs:create       (Create blogs)
├── blogs:update       (Update own blogs)
├── blogs:delete       (Delete own blogs)
├── blogs:publish      (Publish/unpublish)
└── blogs:manage       (Manage all blogs)

ORGANIZATIONS
├── organizations:read      (View orgs)
├── organizations:create    (Create orgs)
├── organizations:update    (Update own orgs)
├── organizations:delete    (Delete own orgs)
└── organizations:manage    (Manage all orgs)

COMMENTS
├── comments:read      (View comments)
├── comments:create    (Post comments)
├── comments:update    (Update own comments)
├── comments:delete    (Delete own comments)
└── comments:moderate  (Moderate all comments)

ADMINISTRATION
├── roles:read             (View roles)
├── roles:manage           (Manage roles)
├── permissions:manage     (Manage permissions)
└── content:moderate       (Moderate all content)
```

## Code Flow Example

### 1. User Tries to Create Blog

```
Request: POST /blogs/new
         │
         ▼
┌────────────────────┐
│  Router Matches    │
│  live "/blogs/new" │
└─────────┬──────────┘
          │
          ▼
┌───────────────────────────┐
│  live_session executes    │
│  on_mount hooks:          │
│  1. :require_authenticated│
│  2. {:require_permission, │
│      "blogs:create"}      │
└─────────┬─────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│  Load user with role            │
│  user = Repo.preload(user,      │
│                     :role)      │
└─────────┬───────────────────────┘
          │
          ▼
┌─────────────────────────────────┐
│  Check permission               │
│  Authorization                  │
│    .user_has_permission?(       │
│      user, "blogs:create")      │
└─────────┬───────────────────────┘
          │
          ▼
    ┌─────┴─────┐
   YES          NO
    │            │
    ▼            ▼
  RENDER      REDIRECT
  FORM        /dashboard
            (with error)
```

### 2. In Template - Show Button Conditionally

```heex
<%!-- Template checks permission --%>
<%= if can?(@current_scope.user, "blogs:create") do %>
  │
  ▼
┌─────────────────────────┐
│ Authorization.can?      │
│ called with user        │
└──────────┬──────────────┘
           │
           ▼
┌────────────────────────────┐
│ Get user's role            │
│ permissions from DB        │
└──────────┬─────────────────┘
           │
           ▼
┌────────────────────────────┐
│ Check if "blogs:create"    │
│ in permission slugs        │
└──────────┬─────────────────┘
           │
           ▼
      ┌────┴────┐
     YES       NO
      │         │
      ▼         ▼
   RENDER    DON'T
   BUTTON    RENDER
```

## Admin Panel Interface

```
┌───────────────────────────────────────────────┐
│          CURATORIAN DASHBOARD                 │
├───────────────────────────────────────────────┤
│                                               │
│  Regular User Menu:                           │
│  ├─ Dashboard                                 │
│  ├─ My Blogs                                  │
│  ├─ Organizations                             │
│  └─ Settings                                  │
│                                               │
│  Manager Menu: (if is_manager?)               │
│  └─ User Management                           │
│                                               │
│  Super Admin Menu: (if is_super_admin?)       │
│  └─ Admin ▼                                   │
│      ├─ Roles Management                      │
│      └─ Permissions Management                │
│                                               │
└───────────────────────────────────────────────┘
```

## Role Management Interface

```
/dashboard/admin/roles
┌─────────────────────────────────────────────┐
│  Role Management              [+ New Role]  │
├─────────────────────────────────────────────┤
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ Super Admin        🔐 System Role    │  │
│  │ Priority: 100                        │  │
│  │ Permissions: 25                      │  │
│  │                    [Edit] [🚫Delete] │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ Manager                              │  │
│  │ Priority: 80                         │  │
│  │ Permissions: 23                      │  │
│  │                    [Edit] [Delete]   │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  ┌──────────────────────────────────────┐  │
│  │ Curator                              │  │
│  │ Priority: 50                         │  │
│  │ Permissions: 13                      │  │
│  │                    [Edit] [Delete]   │  │
│  └──────────────────────────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

## Permission Assignment UI

```
/dashboard/admin/roles/new
┌──────────────────────────────────────────────┐
│  Create New Role                             │
├──────────────────────────────────────────────┤
│  Name: [Content Manager____________]         │
│  Slug: [content_manager____________]         │
│  Description: [_____________________]        │
│  Priority: [60___]                           │
│                                              │
│  Permissions:                                │
│  ┌─ BLOGS ─────────────────────────────┐    │
│  │ ☑ View Blogs (blogs:read)           │    │
│  │ ☑ Create Blogs (blogs:create)       │    │
│  │ ☑ Update Blogs (blogs:update)       │    │
│  │ ☐ Delete Blogs (blogs:delete)       │    │
│  │ ☑ Publish Blogs (blogs:publish)     │    │
│  │ ☐ Manage All Blogs (blogs:manage)   │    │
│  └─────────────────────────────────────┘    │
│                                              │
│  ┌─ USERS ─────────────────────────────┐    │
│  │ ☑ View Users (users:read)           │    │
│  │ ☐ Create Users (users:create)       │    │
│  │ ☐ Update Users (users:update)       │    │
│  │ ☐ Delete Users (users:delete)       │    │
│  │ ☐ Manage Users (users:manage)       │    │
│  └─────────────────────────────────────┘    │
│                                              │
│            [Cancel]  [Save Role]             │
└──────────────────────────────────────────────┘
```

## Data Relationships

```
One User → Has One Role
         ↓
      One Role → Has Many Permissions
                ↓
             Permissions define what actions
             can be performed on resources
```

## Authorization Decision Tree

```
                    START
                      │
                      ▼
              User logged in?
                   ╱   ╲
                 NO     YES
                 │       │
                 ▼       ▼
            REDIRECT  Has role?
            to login   ╱   ╲
                     NO     YES
                     │       │
                     ▼       ▼
                 Assign  Is super_admin?
                default   ╱        ╲
                 role   YES        NO
                     │   │          │
                     │   ▼          ▼
                     │ GRANT    Check specific
                     │ ACCESS   permission
                     │            ╱      ╲
                     │          YES      NO
                     │           │        │
                     └───────────┼────────┘
                                 │        │
                                 ▼        ▼
                              GRANT    DENY
                              ACCESS  ACCESS
```

## Module Dependencies

```
┌──────────────────────┐
│   CuratorianWeb      │
│    (Router)          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│  CuratorianWeb       │
│  .Authorization      │◄────────┐
│  (Plugs & Helpers)   │         │
└──────────┬───────────┘         │
           │                     │
           ▼                     │
┌──────────────────────┐         │
│   Curatorian         │         │
│   .Authorization     │─────────┘
│   (Context)          │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────┐
│  Authorization Schemas:      │
│  - Role                      │
│  - Permission                │
│  - RolePermission            │
└──────────────────────────────┘
```

---

This architecture provides:
- ✅ Separation of concerns
- ✅ Flexible permission management
- ✅ Easy to extend
- ✅ Secure by default
- ✅ User-friendly admin interface
